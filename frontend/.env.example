# Base URL for the MossCart API.
# Leave empty to use Vite's dev proxy (relative /api requests).
# For Cloudflare Workers: VITE_API_BASE_URL=https://mosscart-worker.<your-subdomain>.workers.dev
VITE_API_BASE_URL=""
